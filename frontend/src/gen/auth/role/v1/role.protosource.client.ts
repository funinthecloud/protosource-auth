
// Code generated by protoc-gen-protosource-ts. DO NOT EDIT.
/* eslint-disable */
// @ts-nocheck

import type { ProtosourceClient, CommandResponse, History } from "@protosource/client";
import { CreateSchema, RenameSchema, SetDescriptionSchema, AddFunctionSchema, RemoveFunctionSchema, DeleteSchema, RoleSchema, RoleListSchema, State } from "./role_pb.js";
import type { Create, Rename, SetDescription, AddFunction, RemoveFunction, Delete, Role, RoleList, FunctionGrant } from "./role_pb.js";

const routePath = "auth/role/v1";

export class RoleHTTPClient {
  private readonly client: ProtosourceClient;

  constructor(client: ProtosourceClient) {
    this.client = client;
  }

  async create(id: string, name: string, description: string): Promise<CommandResponse> {
    return this.client.apply(routePath, CreateSchema, { id, name: name, description: description });
  }

  async rename(id: string, name: string): Promise<CommandResponse> {
    return this.client.apply(routePath, RenameSchema, { id, name: name });
  }

  async setDescription(id: string, description: string): Promise<CommandResponse> {
    return this.client.apply(routePath, SetDescriptionSchema, { id, description: description });
  }

  async addFunction(id: string, grant: FunctionGrant): Promise<CommandResponse> {
    return this.client.apply(routePath, AddFunctionSchema, { id, grant: grant });
  }

  async removeFunction(id: string, function_: string): Promise<CommandResponse> {
    return this.client.apply(routePath, RemoveFunctionSchema, { id, function: function_ });
  }

  async delete(id: string): Promise<CommandResponse> {
    return this.client.apply(routePath, DeleteSchema, { id });
  }

  async load(id: string): Promise<Role> {
    return this.client.load(routePath, id, RoleSchema);
  }

  async get(id: string): Promise<Role> {
    return this.client.get(routePath, id, RoleSchema);
  }

  async history(id: string): Promise<History> {
    return this.client.history(routePath, id);
  }

  async queryByState(state: State): Promise<Role[]> {
    const params: Record<string, string> = {
      "state": String(state),
    };
    const list = await this.client.query(routePath, "by-state", params, RoleListSchema);
    return list.items;
  }


  async queryByStateWithCreateAt(state: State, skOp: string, createAt: bigint): Promise<Role[]> {
    if (skOp === "between") {
      throw new Error("use queryByStateBetweenCreateAt for between queries");
    }
    const params: Record<string, string> = {
      "state": String(state),
      "sk_op": skOp,
      "create_at": String(createAt),
    };
    const list = await this.client.query(routePath, "by-state", params, RoleListSchema);
    return list.items;
  }

  async queryByStateBetweenCreateAt(state: State, createAtFrom: bigint, createAtTo: bigint): Promise<Role[]> {
    const params: Record<string, string> = {
      "state": String(state),
      "sk_op": "between",
      "create_at": String(createAtFrom),
      "create_at2": String(createAtTo),
    };
    const list = await this.client.query(routePath, "by-state", params, RoleListSchema);
    return list.items;
  }

}
