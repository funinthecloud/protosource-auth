
// Code generated by protoc-gen-protosource-ts. DO NOT EDIT.
/* eslint-disable */
// @ts-nocheck

import type { ProtosourceClient, CommandResponse, History } from "@protosource/client";
import { IssueSchema, RevokeSchema, TokenSchema, TokenListSchema, State } from "./token_pb.js";
import type { Issue, Revoke, Token, TokenList } from "./token_pb.js";

const routePath = "auth/token/v1";

export class TokenHTTPClient {
  private readonly client: ProtosourceClient;

  constructor(client: ProtosourceClient) {
    this.client = client;
  }

  async issue(id: string, userId: string, issuerId: string, jwt: string, issuedAt: bigint, expiresAt: bigint): Promise<CommandResponse> {
    return this.client.apply(routePath, IssueSchema, { id, userId: userId, issuerId: issuerId, jwt: jwt, issuedAt: issuedAt, expiresAt: expiresAt });
  }

  async revoke(id: string): Promise<CommandResponse> {
    return this.client.apply(routePath, RevokeSchema, { id });
  }

  async load(id: string): Promise<Token> {
    return this.client.load(routePath, id, TokenSchema);
  }

  async get(id: string): Promise<Token> {
    return this.client.get(routePath, id, TokenSchema);
  }

  async history(id: string): Promise<History> {
    return this.client.history(routePath, id);
  }

  async queryByState(state: State): Promise<Token[]> {
    const params: Record<string, string> = {
      "state": String(state),
    };
    const list = await this.client.query(routePath, "by-state", params, TokenListSchema);
    return list.items;
  }


  async queryByStateWithCreateAt(state: State, skOp: string, createAt: bigint): Promise<Token[]> {
    if (skOp === "between") {
      throw new Error("use queryByStateBetweenCreateAt for between queries");
    }
    const params: Record<string, string> = {
      "state": String(state),
      "sk_op": skOp,
      "create_at": String(createAt),
    };
    const list = await this.client.query(routePath, "by-state", params, TokenListSchema);
    return list.items;
  }

  async queryByStateBetweenCreateAt(state: State, createAtFrom: bigint, createAtTo: bigint): Promise<Token[]> {
    const params: Record<string, string> = {
      "state": String(state),
      "sk_op": "between",
      "create_at": String(createAtFrom),
      "create_at2": String(createAtTo),
    };
    const list = await this.client.query(routePath, "by-state", params, TokenListSchema);
    return list.items;
  }

}
